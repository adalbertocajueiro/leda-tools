no pom:
<dependency>
            <groupId>org.jooby</groupId>
            <artifactId>jooby-jade</artifactId>
            <version>1.0.0.CR6</version>
</dependency>

index.jade (na raiz do projeto):
doctype html
html
    head
        title= pageName
    body
        div #{greeting}
        div
            ol#alunos
                for aluno in alunos
                    li #{aluno}

no  SubmissionServer.java:

{
   ...
    use(new Jade());
      
    
    get("/", (req) -> {
        List<String> alunos = new ArrayList<String>();
        
        View html = Results.html("index");
        html.put("pageName", "Teste Leda");
        
        alunos.add("Gustavo Oliveira");
        alunos.add("Adalberto Cajueiro");
        alunos.add("Bernard Silva");        
        
        html.put("greeting","Welcome");
        html.put("alunos", alunos);
        
        return html;
    });
